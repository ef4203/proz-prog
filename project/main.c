#include <libBMP.h>

int main(int argc, char const *argv[])
{
    /* Parse command line arguments. */
    /* Look for the file. */
    /* Load the file. */
    /* Solve the maze. */
    /* Write new file. */
    return 1;
}

